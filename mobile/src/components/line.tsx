import React from 'react';
import { Polyline } from 'react-native-maps';
import MapViewDirections from "react-native-maps-directions";
import { LineProps } from "../types";

const Line = ({ coordinates }: LineProps) => {
    return (
        <>

        </>
    );
};

export default Line;
